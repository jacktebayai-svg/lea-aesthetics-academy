{
  "name": "master-aesthetics-suite",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "db:migrate": "env -u DATABASE_URL pnpm dlx prisma migrate dev --schema packages/db/prisma/schema.prisma",
    "db:generate": "env -u DATABASE_URL pnpm dlx prisma generate --schema packages/db/prisma/schema.prisma",
    "db:seed": "bash -lc 'set -a; source packages/db/prisma/.env; set +a; pnpm dlx ts-node packages/db/prisma/seed.ts'",
    "stack:up": "docker compose up -d db redis meili",
    "stack:down": "docker compose down",
    "test:e2e:playwright": "pnpm dlx playwright test"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.10",
    "prettier": "^3.3.2",
    "turbo": "^2.0.6",
    "@playwright/test": "^1.47.2"
  },
  "packageManager": "pnpm@9.4.0",
  "engines": {
    "node": ">=18"
  }
}
